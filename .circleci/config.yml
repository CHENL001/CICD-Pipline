version: 2.1
jobs:
  build:
    docker:
      - circleci/node:14
    steps:
      - checkout
      - run:
          name: Run tests
          command: |
            npm install
            npm test

